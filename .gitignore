# .NET build output
bin/
obj/
publish/
*.user

# Rider / VS
.vs/
.idea/

# Logs / OS
*.log
Thumbs.db
.DS_Store

# Local working backups (per Jake's safe-access rule)
*.bak
.claude-working/

# Local Claude Code / editor config (not part of the project)
.claude/

# Telemetry session recordings (may contain a user's session data; never commit)
*.fts

# Local VirtualTCU settings backup (personal data, not part of the project)
.vtcu-backup/
